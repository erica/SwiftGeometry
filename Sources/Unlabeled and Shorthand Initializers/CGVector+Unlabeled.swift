/*
 
 erica sadun, ericasadun.com
 Core Geometry
 
 */

import Foundation
import QuartzCore

extension CGVector {
    /// Unlabeled initializer
    public init(_ dx: CGFloat, _ dy: CGFloat) { (self.dx, self.dy) = (dx, dy) }
}

